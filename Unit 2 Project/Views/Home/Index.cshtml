@model List<Employee>
@{
    ViewData["Title"] = "Employees";
}

<!-- Heading -->
<h2>Employee List</h2>

<!-- Table to display employees -->
<a asp-controller="Home" asp-action="Add">Add New Employee</a>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Position</th>
            <th>Salary</th>
        </tr>
    </thead>
    <tbody>
        <!-- Loop through employees -->
        @foreach (var employee in Model)
        {
            <tr>
                <!-- Display employees -->
                <td>@employee.Name</td>
                <td>@employee.Position</td>
                <td>@employee.Salary</td>
                <td>
                    <a asp-controller="Home" asp-action="Edit" asp-route-id="@employee.Id">Edit</a>
                    <a asp-controller="Home" asp-action="Delete" asp-route-id="@employee.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
